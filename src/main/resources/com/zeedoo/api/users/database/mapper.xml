<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeedoo.api.users.database.SqlMapper">
    <resultMap type="com.zeedoo.api.users.domain.User" id="userMap">
        <result column="use_username" property="username"/>
        <result column="use_password" property="password"/>
    </resultMap>
    
    <select id="selectUserByUsername" resultMap="userMap">
        SELECT * FROM users WHERE use_username = #{username}
    </select>
</mapper>